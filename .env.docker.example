# Docker Environment Configuration for Director's Console
# Copy this file to .env and edit for your system:
#   cp .env.docker.example .env

# =============================================================================
# NAS CREDENTIALS (Required for docker-compose.nas.yml)
# =============================================================================
# Your NAS/SMB username and password for mounting network shares
NAS_USER=your_username
NAS_PASS=your_password

# =============================================================================
# PROJECT FOLDERS (For docker-compose.yml - local drives only)
# =============================================================================
# For LOCAL drives only (not network shares):
# PROJECTS_PATH_1=C:/Users/YourName/Projects

# Default (current directory):
PROJECTS_PATH_1=.
PROJECTS_PATH_2=.
PROJECTS_PATH_3=.
PROJECTS_PATH_4=.

# =============================================================================
# COMFYUI NODES (Optional - for Orchestrator job queuing)
# =============================================================================
# Comma-separated list of ComfyUI node addresses
# Only needed if using Orchestrator for job distribution
# Note: The frontend Node Manager uses browser localStorage instead

# COMFY_NODES=192.168.1.100:8188,192.168.1.101:8188
